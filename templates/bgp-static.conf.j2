# {{ ansible_managed }}

{% for ipaddress, subnet, route in bgp_static_configs %}
protocol static static_bgp {
  route {{ ipaddress | indent(2) -}}:{{ subnet | indent(2) -}} via {{ route | indent(2) -}}
}
{% endfor %}
